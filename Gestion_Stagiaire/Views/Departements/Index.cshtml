@model IEnumerable<Gestion_Stagiaire.Models.Departement>

@{
    ViewData["Title"] = "Gestion des Départements";
}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <style>


        .container {
            margin-top: 20px;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }

        h1 {
            text-align: center;
            color: #343a40;
            margin-bottom: 20px;
        }

        .form-inline1 {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: #ffffff;
            padding: 10px 15px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

            .form-inline1 input {
                flex: 1;
                margin-right: 10px;
                padding: 8px 10px;
                border: 1px solid #ccc;
                border-radius: 4px;
                font-size: 14px;
            }

            .form-inline1 button {
                background-color: #2ecc71;
                color: white;
                border: none;
                padding: 8px 15px;
                border-radius: 4px;
                cursor: pointer;
                font-size: 14px;
            }

                .form-inline1 button:hover {
                    background-color: #27ae60;
                }

        .table-container {
            margin-top: 20px;
            background: #ffffff;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

            table th, table td {
                padding: 12px 15px;
                text-align: left;
                border-bottom: 1px solid #ddd;
            }

            table thead {
                background-color: #34495e;
                color: white;
            }

            table tbody tr:hover {
                background-color: #f1f1f1;
            }

        .action-icons {
            display: flex;
            gap: 10px;
        }

            .action-icons a {
                text-decoration: none;
                font-size: 20px;
                color: #3498db;
                transition: color 0.3s;
            }

                .action-icons a:hover {
                    color: #2c3e50;
                }


        /* Style commun pour les entités (départements/types de stage) */
        .entity-name {
            font-weight: bold;
            font-size: 1.2rem;
            color: #2c3e50;
            font-style: italic;
            text-transform: capitalize;
            letter-spacing: 1px;
        }

        /* Bouton Ajouter comme icône */
        .create-btn1 {
            background-color: #2ecc71;
            padding: 10px 15px;
            font-size: 0.5rem;
            text-align: center;
            border-radius: 50%;
            width: 60px;
            height: 60px;
            display: inline-block;
            text-decoration: none;
            color: white;
            font-weight: bold;
            margin-top: 10px;
            transition: all 0.3s ease;
        }

            .create-btn1 i {
                font-size: 1.8rem;
            }

            .create-btn1:hover {
                background-color: #27ae60;
            }
    </style>
    <!-- Ajout d'une icône FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div class="container">
        <!-- Formulaire pour créer un département -->
        <div class="form-container mb-4">
            <h1>@ViewData["Title"]</h1>
            <form asp-action="Create">
                <div class="mb-3">
                    <input type="text" class="form-control" id="Nom_Departement" name="Nom_Departement" placeholder="Saisir le nom du département">
                </div>
                <button type="submit" class="btn btn-success">
                    <i class="bi bi-plus-circle"></i> Ajouter
                </button>
            </form>
        </div>

        <!-- Tableau des départements -->
        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Nom du Département</th>
                        <th>Modifier</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="entity-name">@Html.DisplayFor(modelItem => item.Nom_Departement)</td>
                            <td class="action-icons">
                                <a asp-action="Edit" asp-route-id="@item.Id" title="Modifier"><i class="fas fa-edit"></i></a>

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>


</body>
</html>
