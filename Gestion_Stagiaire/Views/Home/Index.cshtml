@{
    ViewData["Title"] = "Page d'accueil";
}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
    <!-- Font Awesome (pour les icônes) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="~/css/styles.css" rel="stylesheet" />
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            
            color: #333;
            margin: 0;
            padding: 0;
        }

        /* Section Hero */
        .hero {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 60px 50px; /* Diminuer le padding pour rapprocher du haut */
            text-align: left;
            animation: fadeIn 1.5s ease-in-out;
            margin-top: -200px; /* Déplacer vers le haut */
        }

        .hero-text {
            max-width: 500px;
        }

        .hero-text h1 {
            font-size: 3.5rem;
            font-weight: 700;
            color: #333;
            line-height: 1.2;
            margin-bottom: 20px;
        }

        .hero-text p {
            font-size: 1.2rem;
            font-weight: 300;
            margin-top: 15px;
        }

        .hero a {
            text-decoration: none;
            color: #fff;
            background-color: #1d9f58;
            padding: 12px 30px;
            font-size: 1.2rem;
            border-radius: 8px;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .hero a:hover {
            background-color: #0e7b40;
            transform: scale(1.05);
        }

        /* Section Conteneur */
        .container {
            margin-top: 40px; /* Maintenir un léger espacement */
            text-align: center;
        }

        .card {
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            transition: box-shadow 0.3s ease, transform 0.3s ease;
            margin: 20px;
        }

        .card:hover {
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            transform: translateY(-10px);
        }

        .card h3 {
            font-size: 1.6rem;
            color: #333;
            font-weight: 500;
        }

        .card p {
            font-size: 1.1rem;
            color: #555;
        }

        .cta-btn {
            background-color: #10b981;
            color: #fff;
            padding: 12px 30px;
            border-radius: 5px;
            font-size: 1.2rem;
            text-decoration: none;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .cta-btn:hover {
            background-color: #059669;
            transform: scale(1.05);
        }

        /* Section Image */
        .image-container {
            flex: 1;
            max-width: 50%;
        }

        .image-container img {
            width: 100%;
            height: auto;
            border-radius: 8px;
            transition: transform 0.3s ease;
        }

        .image-container img:hover {
            transform: scale(1.05);
        }

    </style>
</head>
<body>

<!-- Section Hero avec Image -->
<section class="hero">
    <div class="hero-text">
        <h1>Bienvenue sur notre plateforme de stages</h1>
        <p>Explorez des opportunités de stage passionnantes dans l'industrie textile. Rejoignez-nous et donnez un coup de pouce à votre carrière dès aujourd'hui !</p>
        <a href="/internships" class="cta-btn">Explorer les Stages</a>
    </div>
    <div class="image-container">
        <img src="images/Image2.png" alt="Stage Textile" />
    </div>
</section>

<!-- Section des Cartes -->
<div class="container">
    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <h3>Pour les Stagiaires</h3>
                <p>Trouvez le stage qui correspond à vos compétences et à vos intérêts. Commencez votre parcours professionnel avec nous !</p>
                <a href="/internships" class="cta-btn">Voir les Opportunités</a>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card">
                <h3>Pour les Ressources Humaines</h3>
                <p>Gérez les candidatures de stage, suivez les progrès et recrutez des talents parmi un groupe d'internes qualifiés.</p>
                <a href="/admin" class="cta-btn">Gérer les Stages</a>
            </div>
        </div>
    </div>
</div>



</body>
</html>

